{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability set for the empty desktop shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
